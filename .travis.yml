language: r

cache: packages

matrix:
  include:
    - os: linux
      dist: precise
      sudo: false
      r: 3.3.3
    - os: linux
      dist: precise
      sudo: false
      r: release
    - os: linux
      dist: precise
      sudo: false
      r: devel
    - os: osx
      r: 3.3.3
    - os: osx
      r: release
    - os: osx
      r: devel

notifications:
  email:
      on_failure: mdoering@mpi-inf.mpg.de
